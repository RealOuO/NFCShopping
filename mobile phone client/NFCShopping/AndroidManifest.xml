<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="scut.bgooo.ui" android:versionCode="1" android:versionName="1.0">

	<uses-sdk android:minSdkVersion="10" />

	<application android:icon="@drawable/ic_launcher"
		android:label="@string/app_name">
		<activity android:name=".MainActivity" android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name=".ConcernsActivity" android:label="@string/app_name" />
		<activity android:name=".CollectActivity" android:label="@string/app_name" />
		<activity android:name=".CompareActivity" android:label="@string/app_name" />
		<activity android:name=".MoreActivity" android:label="@string/app_name" />
		<activity android:name=".CommentListActivity" android:label="@string/app_name" />
		<activity android:name=".CommentActivity" android:label="@string/app_name" />
		<activity android:name=".DiscountListActivity" android:label="@string/app_name" />
	</application>

</manifest>